# ClawStreetBot v2 — Infrastructure
# All config flows through .env (gitignored) — never hardcode credentials here.

services:
  # ── Data Layer ──────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: cbs-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cbs}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:-clawstreetbot}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-cbs}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Cache & Pub/Sub ─────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: cbs-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:?Set REDIS_PASSWORD in .env} --appendonly yes
    environment:
      # REDISCLI_AUTH lets redis-cli auth without leaking password in process list
      REDISCLI_AUTH: ${REDIS_PASSWORD}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      # Use REDISCLI_AUTH env var instead of -a flag to avoid password in process list
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Workflow Engine ──────────────────────────────────────────
  n8n:
    image: n8nio/n8n:latest
    container_name: cbs-n8n
    restart: unless-stopped
    environment:
      # n8n 2.x uses N8N_AUTH_* (N8N_BASIC_AUTH_* is deprecated)
      N8N_AUTH_ACTIVE: "true"
      N8N_AUTH_USERNAME: ${N8N_USER:-admin}
      N8N_AUTH_PASSWORD: ${N8N_PASSWORD:?Set N8N_PASSWORD in .env}
      N8N_HOST: ${N8N_HOST:-localhost}
      N8N_PORT: 5678
      N8N_PROTOCOL: http
      N8N_EDITOR_BASE_URL: ${N8N_EDITOR_BASE_URL:-}
      WEBHOOK_URL: ${N8N_WEBHOOK_URL:-}
      # n8n uses its own database within our postgres, not shared with app data
      DB_TYPE: postgresdb
      DB_POSTGRESDB_HOST: postgres
      DB_POSTGRESDB_PORT: 5432
      DB_POSTGRESDB_DATABASE: ${POSTGRES_DB:-clawstreetbot}
      DB_POSTGRESDB_USER: ${POSTGRES_USER:-cbs}
      DB_POSTGRESDB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_POSTGRESDB_SCHEMA: n8n
      GENERIC_TIMEZONE: ${TZ:-America/Los_Angeles}
    ports:
      - "${N8N_PORT:-5678}:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    depends_on:
      postgres:
        condition: service_healthy

  # ── Obsidian Knowledge Base ──────────────────────────────────
  # NOTE: seccomp:unconfined is required for Electron apps in Docker.
  # This container should NOT be exposed to the internet without a
  # reverse proxy (e.g., SWAG, Caddy) with proper authentication.
  obsidian:
    image: lscr.io/linuxserver/obsidian:latest
    container_name: cbs-obsidian
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1001}
      PGID: ${PGID:-1001}
      TZ: ${TZ:-America/Los_Angeles}
      CUSTOM_USER: ${OBSIDIAN_USER:-cbs}
      PASSWORD: ${OBSIDIAN_PASSWORD:?Set OBSIDIAN_PASSWORD in .env}
    ports:
      - "${OBSIDIAN_HTTP_PORT:-3000}:3000"
      - "${OBSIDIAN_HTTPS_PORT:-3001}:3001"
    volumes:
      - obsidian_config:/config
      - ./docs/obsidian:/config/obsidian/vaults/ClawStreetBot
    shm_size: "1gb"
    security_opt:
      - seccomp=unconfined

  # ── Layer Containers ─────────────────────────────────────────
  # Uncomment and build each layer as code is written.

  # 01_data:
  #   build:
  #     context: .
  #     dockerfile: docker/Dockerfile.layer
  #     args:
  #       LAYER: 01_data
  #   container_name: cbs-01-data
  #   restart: unless-stopped
  #   env_file: .env
  #   volumes:
  #     - ./config:/app/config:ro
  #     - ./shared:/app/shared:ro
  #     - ./01_data:/app/layer
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  n8n_data:
  obsidian_config: